import mongoengine
import components

# from tags.model import Tag

class Category(components.BaseModel):
    # title = mongoengine.StringField(max_length=512)
    # parent = mongoengine.ReferenceField(Category, default=None)
    # children = mongoengine.ListField(mongoengine.ReferenceField(Category), default=[])
    # decoration = ? like colors, etc
    pass

# Other type of notes and its extensions goez here
